import React from 'react';

const Header: React.FC = () => { // React.FC 表示這是一個 React 函式元件
  return (
    <header>
      {/* 導覽列內容稍後添加 */}
      <p>My Header</p>
    </header>
  );
};

export default Header;

